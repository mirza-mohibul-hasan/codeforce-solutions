/*Mirza Mohibul Hasan*/
/*CSE-19@JUST*/
#include <bits/stdc++.h>
using namespace std;
#define pi acos(-1)
#define mod 1000000007
#define ll long long
#define li long int
#define ld long double
#define pb push_back
#define mp make_pair
#define ff first
#define ss second
#define nl "\n"
#define fast_run ios_base::sync_with_stdio(false), cin.tie(nullptr), cout.tie(nullptr);

// int maxSize(string s)
// {
//     unordered_set<char> set;
//     for (int i = 0; i < s.length(); i++)
//     {
//         set.insert(s[i]);
//     }
//     return set.size();
// }
int main()
{
    fast_run;
    int t;
    cin >> t;
    while (t--)
    {
        int n;
        string s;
        cin >> n >> s;
        // int max_len = 0;
        // for (int i = 1; i < n; i++)
        // {
        //     max_len = max(max_len, maxSize(s.substr(0, i)) + maxSize(s.substr(i)));
        // }
        // cout << max_len << endl;
        for(int i=0; i<n; i++){
            
        }
        cout<<n-count+1<<nl;
    }
    return 0;
}
